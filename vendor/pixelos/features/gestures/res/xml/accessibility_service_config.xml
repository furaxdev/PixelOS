<?xml version="1.0" encoding="utf-8"?>
<!--
  This service doesn't need to observe accessibility events or explore-by-touch — it only exists
  to hold BIND_ACCESSIBILITY_SERVICE so it's allowed to call performGlobalAction(). Kept minimal
  on purpose: canRetrieveWindowContent etc. are all omitted since nothing here reads screen content.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestFilterKeyEvents"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />
